<UserControl x:Class="SC2LiquipediaStatistics.DesktopClient.View.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SC2LiquipediaStatistics.DesktopClient.View"
             xmlns:DesktopClient="clr-namespace:SC2LiquipediaStatistics.DesktopClient"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Height="100" Command="{Binding UpdateDatabaseCommand}">Update DataBase</Button>
        <Button Grid.Column="1" Height="100" Command="{Binding EventsListCommand}">See Events List</Button>
        <Button Grid.Column="2" Height="100" Command="{Binding PlayerStatisticsCommand}">Player Statistics</Button>
    </Grid>
</UserControl>
