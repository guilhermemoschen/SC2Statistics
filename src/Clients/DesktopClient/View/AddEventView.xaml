<UserControl x:Class="SC2LiquipediaStatistics.DesktopClient.View.AddEventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             DataContext="{Binding AddEventViewModel, Source={StaticResource Locator}}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel Style="{StaticResource Form}">
                <TextBlock Text="Add New Tournament" Style="{StaticResource Heading2}" />
                <StackPanel>
                    <Label Content="Link" />
                    <TextBox Width="150" Text="{Binding EventUrl}"/>
                </StackPanel>
                <StackPanel>
                    <Button Command="{Binding AddNewCommand}" Content="Add Event"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Width="auto" Height="auto" Margin="0,10,0,0">
                    <TextBlock Text="{Binding LogMessage}" TextWrapping="WrapWithOverflow" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
